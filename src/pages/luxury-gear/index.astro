---
import BaseLayout from '@layouts/BaseLayout.astro';
import PostCard from '@components/PostCard.astro';
import { getPostsForCategory, getSlugFromId } from '@utils/collection-helpers';

const posts = await getPostsForCategory('luxury-gear');
---

<BaseLayout title="Luxury & Gear" description="Premium products and gear for dogs with refined taste.">
  <section class="category-page container">
    <header class="category-header">
      <h1>Luxury & Gear</h1>
      <p>Premium products for dogs with refined taste.</p>
    </header>
    {posts.length > 0 ? (
      <div class="posts-grid">
        {posts.map((post) => (
          <PostCard
            title={post.data.title}
            description={post.data.description}
            href={`/luxury-gear/${getSlugFromId(post.id)}`}
            category={post.data.category}
            image={post.data.heroImage}
          />
        ))}
      </div>
    ) : (
      <p class="empty">New luxury gear reviews coming soon!</p>
    )}
  </section>
</BaseLayout>

<style>
  .category-page {
    padding-top: var(--space-3xl);
    padding-bottom: var(--space-4xl);
  }

  .category-header {
    text-align: center;
    margin-bottom: var(--space-2xl);
  }

  .category-header h1 {
    font-size: var(--text-4xl);
    margin-bottom: var(--space-sm);
  }

  .category-header p {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xl);
  }

  .empty {
    text-align: center;
    color: var(--color-text-muted);
    font-size: var(--text-lg);
  }

  @media (max-width: 1024px) {
    .posts-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .posts-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
